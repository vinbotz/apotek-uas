// Middleware untuk check authentication (disederhanakan)
// Dalam production, gunakan session/JWT yang lebih aman

// Untuk saat ini, kita akan handle auth di frontend saja
// dan validasi role di setiap route jika diperlukan

module.exports = {
  // Middleware ini bisa digunakan jika diperlukan validasi di backend
  // Untuk sekarang kita akan handle di frontend
};
